// store is placeholder for file name
import store from './components/store.js';

const init = () => {
    // do stuff

    // inside store there is a makeStore function
    store.makeStore();
};

init ();